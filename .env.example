# PSPManager — Production Environment
# Copy to .env and fill in values. NEVER commit .env to git.

# JWT Secrets (generate each with: openssl rand -base64 64)
JWT_ACCESS_SECRET=REPLACE_WITH_STRONG_SECRET_1
JWT_REFRESH_SECRET=REPLACE_WITH_STRONG_SECRET_2

# Cookie domain (your root domain, e.g. pspmanager.com)
COOKIE_DOMAIN=yourdomain.com

# Frontend URL (used for CORS)
FRONTEND_URL=https://yourdomain.com

# Next.js frontend → API URL (internal Docker network)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
